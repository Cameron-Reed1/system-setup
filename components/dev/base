#!/usr/bin/env bash

dependencies=""
packages="base-devel clang cmake gcc git go lld llvm make ninja rustup zig"

config="pycodestyle,zls.json"


function post_install() {
    if [ "$(rustup toolchain list)" = "no installed toolchains" ]; then
        rustup default stable
    fi
}
